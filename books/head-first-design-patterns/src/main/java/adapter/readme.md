# 适配器
## [如何命名适配器名称](https://chatgpt.com/share/68eb791a-5250-8006-bf72-529c808f69db)
```java
public class TurkeyAdapter implements Duck {
    private Turkey turkey;

    public TurkeyAdapter(Turkey turkey) {
        this.turkey = turkey;
    }

    @Override
    public void quack() {
        turkey.gobble();
    }

    @Override
    public void fly() {
        for(int i = 0; i < 5; i++) {
            turkey.fly();
        }
    }
}
```
这个适配器的名称是火鸡适配器，是不是不怎么好，因为我看这个适配器的名字不知道适配的什么?

你非常敏锐 👍——这个问题其实触及了**适配器模式的命名语义核心**。

我们来看一下这个类的语义：

```java
public class TurkeyAdapter implements Duck
```

这行字其实已经说明了两个关键角色：

* `Duck` 是**目标接口**（Target）——客户端希望使用的接口。
* `Turkey` 是**被适配者**（Adaptee）——已有的不兼容类型。

---

### 💡 从语义上看，“TurkeyAdapter”这个名字的问题是：

> 它强调了**被适配者（Turkey）**，而忽略了**适配成什么（Duck）**。

当你只看到 `TurkeyAdapter` 时，确实不清楚：

> 这个适配器是把火鸡适配成什么？是适配成鸭子吗？还是别的？

---

### 🧠 更好的命名思路

适配器类的命名应该体现：

> “让什么看起来像什么”。

也就是：

> “Adaptee” → “Target”

因此更清晰的命名方式包括：

| 命名                      | 含义                    | 可读性              |
| ----------------------- | --------------------- | ---------------- |
| `TurkeyToDuckAdapter`   | 把 `Turkey` 适配成 `Duck` | ✅ 明确             |
| `DuckAdapterFromTurkey` | 从 `Turkey` 生成一个鸭子适配器  | ✅ 明确但略冗长         |
| `DuckLikeTurkeyAdapter` | 让火鸡像鸭子一样              | ✅ 有语义感（在教学代码中常见） |
| `DuckAdapter`           | 如果上下文已经明显（比如只有一种适配方向） | ⚠️ 可接受但略模糊       |

---

### ✅ 推荐命名

在教学或工程代码里，最清晰的命名通常是：

```java
public class TurkeyToDuckAdapter implements Duck
```

它清楚地表达：

> “我让火鸡看起来像鸭子一样能被当作鸭子使用”。

---

### 🚀 小结

| 命名目标           | 推荐做法                        |
| -------------- | --------------------------- |
| 表明“谁变成了谁”      | 使用 “AdapteeToTargetAdapter” |
| 若上下文清晰（只有一种方向） | 可用 “TargetAdapter”          |
| 避免只写被适配者名      | 因为看不出目标类型                   |

---

如果你的项目中还有其他类似的适配（例如 `ChickenAdapter implements Bird`、`PenguinAdapter implements Duck`），那更应该使用 `TurkeyToDuckAdapter` 这样的命名方式，方便别人快速看出**适配方向**。

是否希望我帮你总结一份适配器命名的通用规则表（包括双向适配器或多接口适配的情况）？

---

我看到设计模式里提到一个双向适配器，能说下吗

非常好，这个问题切得很深。👍
“双向适配器”（Two-Way Adapter）是**适配器模式（Adapter Pattern）的一种进阶变体**，在某些系统中非常有用——尤其是在**两种接口需要互操作**的场景。

---

## 🧩 一、回顾：普通（单向）适配器

普通的适配器是**单向的**，只解决一种方向的不兼容。

例如：

```java
public class TurkeyToDuckAdapter implements Duck {
    private Turkey turkey;

    public TurkeyToDuckAdapter(Turkey turkey) {
        this.turkey = turkey;
    }

    @Override
    public void quack() {
        turkey.gobble();
    }

    @Override
    public void fly() {
        for (int i = 0; i < 5; i++) turkey.fly();
    }
}
```

* **目标接口 (Target)**：`Duck`
* **被适配者 (Adaptee)**：`Turkey`
* **适配方向**：`Turkey` → `Duck`

此时火鸡可以“伪装成鸭子”，但**鸭子不能伪装成火鸡**。

---

## 🔁 二、双向适配器（Two-Way Adapter）

双向适配器的目标是让：

> **Duck 和 Turkey 都能通过同一个适配器互相替代使用。**

也就是说：

* 你可以把 Duck 当作 Turkey 用；
* 也可以把 Turkey 当作 Duck 用；
* 而两者的调用都通过同一个适配器类实现。

---

## 💻 三、实现示例

假设接口如下：

```java
public interface Duck {
    void quack();
    void fly();
}

public interface Turkey {
    void gobble();
    void fly();
}
```

实现双向适配器：

```java
public class TwoWayAdapter implements Duck, Turkey {
    private Duck duck;
    private Turkey turkey;

    public TwoWayAdapter(Duck duck) {
        this.duck = duck;
    }

    public TwoWayAdapter(Turkey turkey) {
        this.turkey = turkey;
    }

    // --- 当作 Duck 使用时 ---
    @Override
    public void quack() {
        if (turkey != null) {
            turkey.gobble();
        } else {
            duck.quack();
        }
    }

    @Override
    public void fly() {
        if (turkey != null) {
            for (int i = 0; i < 5; i++) turkey.fly();
        } else {
            duck.fly();
        }
    }

    // --- 当作 Turkey 使用时 ---
    @Override
    public void gobble() {
        if (duck != null) {
            duck.quack();
        } else {
            turkey.gobble();
        }
    }
}
```

这样：

* 你可以传入一个 `Turkey`，得到一个 `Duck`；
* 也可以传入一个 `Duck`，得到一个 `Turkey`。

---

## 🧠 四、用途与典型场景

双向适配器通常用于：

1. **旧系统与新系统并存时的兼容**
   例如旧系统用 `OldLogger` 接口，新系统用 `NewLogger` 接口；
   双向适配器让两者都能互相调用，方便平滑迁移。

2. **双系统互通协议或 API 转换**
   比如网络通信两端协议略有不同，可通过双向适配器桥接。

3. **测试或过渡性封装层**
   在重构期间，用双向适配器兼容新旧代码。

---

## ⚖️ 五、优缺点分析

| 优点               | 缺点                 |
| ---------------- | ------------------ |
| 能实现双向兼容，无需两个适配器类 | 代码逻辑复杂，职责模糊        |
| 适合系统迁移过渡阶段       | 违反单一职责原则（同时适配两个接口） |
| 减少类数量            | 可读性差，不利于长期维护       |

---

## ✅ 六、实践建议

| 情况          | 建议                |
| ----------- | ----------------- |
| 系统长期存在      | 用**两个单向适配器**，职责清晰 |
| 系统迁移、测试期    | 可用**双向适配器**临时过渡   |
| 逻辑非常对称、接口很小 | 双向适配器是合理的方案       |

---

### 🧭 举个现实类比：

> 单向适配器就像“翻译官A：只懂英语→中文”；
> 双向适配器则是“翻译官B：既懂英语→中文，也懂中文→英语”。

翻译官B更方便，但脑子更乱（复杂度更高 😄）。

---

是否希望我帮你画一张“双向适配器”结构图（类之间的关系）？会更直观地看到它是如何双向适配的。


---

适配器模式使用对象组合，以修改的接口包装被适配者。 以修改的接口包装被适配者是什么意思

这个问题问得非常好 👍 ——
这句话“**以修改的接口包装被适配者**”其实是描述**适配器模式的核心思想**，但这句话如果不拆解，很容易让人糊涂。
我们一步步拆开解释它的真正含义👇

---

## 🧩 一、背景回顾：适配器模式要解决什么问题？

适配器模式（Adapter Pattern）要解决的是：

> **两个接口不兼容，但又必须一起工作的情况。**

比如：

```java
interface Duck {
    void quack();
    void fly();
}

interface Turkey {
    void gobble();
    void fly();
}
```

现在系统希望**用鸭子的接口（Duck）来操作一只火鸡（Turkey）**。
但是火鸡的接口和鸭子不一样——`quack()` 对应 `gobble()`。
这时你就需要一个中间层 —— “适配器（Adapter）”。

---

## 🧠 二、“以修改的接口包装被适配者”是什么意思？

我们拆解这句话：

| 短语                            | 含义                                                |
| ----------------------------- | ------------------------------------------------- |
| **被适配者（Adaptee）**             | 原本已经存在、但接口不兼容的类（例如 `Turkey`）                      |
| **修改的接口（Modified Interface）** | 客户端希望使用的目标接口（例如 `Duck`）                           |
| **包装（Wrap）**                  | 通过组合，把 `Adaptee` 的对象“包”在 `Adapter` 里面，让调用者看不到原始接口 |

所以整句话的意思是：

> **“适配器用目标接口（Duck）定义对外行为，内部持有被适配者（Turkey）的引用，通过调用 Turkey 的方法来实现 Duck 接口。”**

也就是：

> **外壳换了（接口变了），但内部还是原来的对象。**

---

## 🧩 三、用代码直观理解

```java
// Duck 是目标接口 —— 客户端想要使用的形式
public interface Duck {
    void quack();
    void fly();
}

// Turkey 是被适配者 —— 已有接口，不兼容
public interface Turkey {
    void gobble();
    void fly();
}

// 适配器：修改接口（Duck）包装被适配者（Turkey）
public class TurkeyToDuckAdapter implements Duck {
    private Turkey turkey;  // ← 包装：适配器内部“组合”一个被适配者

    public TurkeyToDuckAdapter(Turkey turkey) {
        this.turkey = turkey;
    }

    // 用Duck的方式（quack）调用Turkey的gobble
    @Override
    public void quack() {
        turkey.gobble();
    }

    // 用Duck的fly接口，包装Turkey的fly行为
    @Override
    public void fly() {
        for (int i = 0; i < 5; i++) turkey.fly();
    }
}
```

👉 这就是那句话的具体体现：

> “以修改的接口（Duck）包装被适配者（Turkey）”

* **“修改的接口”** → 是 `Duck`（客户端所需的接口）。
* **“包装”** → 是 `TurkeyToDuckAdapter` 持有一个 `Turkey` 对象（对象组合）。
* **“被适配者”** → 是 `Turkey`（旧接口的类）。

---

## 🧱 四、为什么说“对象组合”而不是“继承”？

适配器模式有两种实现方式：

1. **对象适配器（推荐）**：通过“组合”一个被适配者对象。
2. **类适配器（少见）**：通过“继承”被适配者。

“对象组合”的好处：

* 不修改原类；
* 可以在运行时动态替换被适配者；
* 更符合组合优于继承原则。

---

## 🎯 五、用一句话总结整句话的真实含义

> **适配器模式通过对象组合，把一个已有对象（被适配者）封装进一个新类中，并通过实现目标接口（修改后的接口），让原对象在不改变自身代码的前提下，能以新接口形式被使用。**

---

### 🧩 小比喻（更形象）

就像你买了一个**三脚插头（Turkey）**，
但家里的插座是**两孔插口（Duck）**。
于是你买了一个**转换插头（Adapter）**：

* 转换插头外表符合两孔插口的形状（Duck 接口）；
* 内部却接着三脚插头（Turkey 对象）；
* 你插上去后，电照样通了（行为被“包装”兼容）。

---

是否希望我帮你画一张图，用 UML 展示这个“包装”关系（Duck、Turkey、TurkeyToDuckAdapter 三者之间的箭头）？会非常清楚“谁包装了谁”。
